alarm_set(0, 120);

// Initialize the variables
stopped = false;
speed = 2;


slow_speed = 0.5;           // Slowed speed
current_speed = speed;
slow_timer = 0;

// setting amount of hearts
big_health = 200;
